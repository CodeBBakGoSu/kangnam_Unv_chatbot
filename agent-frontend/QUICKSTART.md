# 빠른 시작 가이드

프론트엔드를 로컬에서 실행하고 테스트하는 방법입니다.

## 🚀 로컬 실행

### 1단계: 환경 변수 설정
```bash
cd agent-frontend

# .env.local 파일 생성 (이미 있다면 건너뛰기)
echo "REACT_APP_API_URL=https://agent-backend-api-88199591627.us-east4.run.app" > .env.local
```

### 2단계: 의존성 설치 (최초 1회)
```bash
npm install
```

### 3단계: 개발 서버 실행
```bash
npm start
```

자동으로 브라우저가 열립니다: http://localhost:3000

## ✅ 테스트 체크리스트

### 기본 기능
- [ ] 페이지 로드 시 자동으로 "강남대학교 챗봇 강냉이" 화면 표시
- [ ] 좌측 상단에 "+ New" 버튼 표시
- [ ] 추천 질문 3개 표시

### 채팅 기능
- [ ] 추천 질문 클릭 시 입력창에 자동 입력
- [ ] 메시지 입력 및 전송 (Enter 키 or 전송 버튼)
- [ ] 스트리밍 응답 실시간 표시 (글자가 차례대로 나타남)
- [ ] 로딩 중 전송 버튼 비활성화 (⏳ 아이콘 표시)

### New 버튼
- [ ] "New" 버튼 클릭 시 채팅 내역 초기화
- [ ] 첫 화면으로 돌아가기
- [ ] 새 세션 자동 생성

### 오류 처리
- [ ] 백엔드 API 오류 시 에러 메시지 표시
- [ ] 네트워크 오류 시 적절한 메시지 표시

## 🔍 디버깅

### 브라우저 개발자 도구
F12 또는 Cmd+Option+I (Mac):

**Console 탭:**
- 세션 생성 로그 확인
- API 호출 에러 확인

**Network 탭:**
- `/chat/new` 요청 확인 (POST)
- `/chat/message` 요청 확인 (POST, SSE 스트리밍)
- 응답 상태 코드 확인 (200 OK)

## 🐛 문제 해결

### "REACT_APP_API_URL is not defined"
```bash
# .env.local 파일 확인
cat .env.local

# 없으면 생성
echo "REACT_APP_API_URL=https://agent-backend-api-88199591627.us-east4.run.app" > .env.local

# 서버 재시작
npm start
```

### "Failed to create chat"
백엔드 API가 실행 중인지 확인:
```bash
curl https://agent-backend-api-88199591627.us-east4.run.app/health
# 응답: {"status":"ok","service":"agent-backend-api"}
```

### 포트 3000이 이미 사용 중
다른 포트로 실행:
```bash
PORT=3001 npm start
```

### 스트리밍 응답이 한번에 나타남
- Chrome 개발자 도구 Network 탭 확인
- Response가 chunked transfer encoding인지 확인
- 백엔드에서 SSE 포맷 확인

## 📦 프로덕션 빌드

### 빌드
```bash
npm run build
```

`build/` 폴더에 최적화된 파일 생성

### 로컬에서 프로덕션 빌드 테스트
```bash
# serve 설치 (최초 1회)
npm install -g serve

# 빌드된 파일 서빙
serve -s build -p 3000
```

## 🚀 다음 단계

로컬 테스트 완료 후:
1. **Vercel 배포**: `VERCEL_DEPLOY.md` 참고
2. **GitHub 연동**: 자동 배포 설정

## 📝 주요 파일

- `src/AgentChat.jsx`: 메인 채팅 컴포넌트
- `src/services/chatService.js`: API 통신 로직
- `.env.local`: 환경 변수 (백엔드 URL)
- `vercel.json`: Vercel 배포 설정

---

**문제가 있으면 브라우저 콘솔을 먼저 확인하세요!** 🔍

